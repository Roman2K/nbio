refactor @reads, @writes, prom_maps

error_array => prom.reject

non-blocking
  TCPSocket.new
  close

io_loop.fs. 
  http://nodejs.org/api/fs.html

stop
  examples/server_accept.rb

Streams::Write
  hwm => node/lib/_stream_writable.js

close event (?)
